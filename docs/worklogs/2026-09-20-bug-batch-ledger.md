# Bug batch 2026-09-20 — classification ledger (issues #1841, #1824, and the #1841 blast radius)

Branch: fix/bug-batch-sep20. Base: origin/main (e0b2ff84).

Batched because the two open `bug` issues were the whole open-bug queue at
`e0b2ff84`, and #1841's acceptance criterion is a green oracle-replay nightly,
which only the blast-radius enumeration below can confirm.

## Ledger

| Finding | Classification | Evidence (working hash) | Plan | Status |
|---|---|---|---|---|
| #1841 `matrix_norm_c128_identity_002` VJP: `execute_elementwise_fusion: dtype mismatch: expected C64, actual F64` | Auto Fix | `segment.rs:1441` `build_elementwise_fusion_plan` compares `inst.dtype` only, and `inst.dtype` is the inferred **output** dtype (`program/builder.rs:394` → `shape_infer.rs:149`). Region reproduced with instrumentation: `Abs` (dtype F64, complex region input) + `BroadcastInDim` + `Multiply` → plan dtype F64, input dtypes `[C64, F64, F64]`. The CPU kernel's dtype check (`tenferro-cpu-fused/src/lib.rs:63-70`) runs before its `ELEMENTWISE_FUSION_MIN_ELEMENTS` deferral, so an all-real plan over a complex input is a hard error instead of a decline. Reachable in production only since the prepare-time region executor (`80ca4a4a`, `f0b67cf2`); the segmented executor that already used the plan builder is test-only (`65edc91c`). | Decline the fusion when a region input does not share `plan.dtype()` and fall back to per-instruction execution (`segment.rs:1497` helper; region path `runtime/execution.rs:1339`; segmented path `segment.rs:819/870`). Regression test at the segment level: `segment/tests.rs:104`. | Fix |
| #1841 blast radius: `eigh_c128_values_vectors_abs_001/005/007` probe p0 JVP value mismatch | Auto Fix | Unmasked by the fix above; the runner stops at the first failure, so the remaining 2149 records had never been replayed. All three fail on unmodified `origin/main` (verified with a stashed working tree). Root cause: `linearize_eigh_values` projected `diag(V^H dA V)` as `rowsum(V^H ∘ (V^H dA))` (`mod.rs:1101` before this change, added by #1835), but the rowwise factor is `conj(V^H) = V^T`. Real dtypes cannot see the difference, and the rule's real-only finite-difference test passed. | Use `transpose_matrix_fixed(v)` as the Hadamard factor (`mod.rs:1101`); both directions stay correct because the transposed path conjugates the fixed operand. Regression test with a complex Hermitian input and an exact reference (`d(sum λ) = tr(dA)`): `traced_ad_explicit.rs:735`. | Fix |
| #1824 `set_placement` lost through `AllocationGroup::from_tensors` → `take_tensor` / `read_view` | Auto Fix | `types.rs` `TensorCore.placement` is what `set_placement` writes and `placement()` reads, while the group descriptor is seeded `Placement::default()` (`storage/group.rs:935`) and only written by the backend-buffer constructors. `into_group_parts` dropped the core copy, and `take_tensor`/`read_view` rebuild from the descriptor (`storage/group.rs:1476`, `:1547`). The existing round-trip test at `storage/tests/group.rs:570` uses a default placement, so it could not see the loss. | Publish the core placement into the live descriptor before the handover (`types.rs:4025` via `storage/group.rs:724`). This is the minimal alternative named in the issue; the single-owner redesign and the 80 B `Tensor` shrink stay with #1823. Regression test: `storage/tests/group.rs:534`. | Fix |

## Verification record

- `RUST_TEST_THREADS=1 RUN_ORACLE_REPLAY=1 ORACLE_REPLAY_JOBS=4 cargo test -p tenferro-linalg --features autodiff --test integration oracle_replays_supported_db_cases_when_requested` → `ok`, 2150 replayed success + 2 expected-error, 0 failures, with the unmodified fail-fast harness. Before the batch: 1 failure (dtype mismatch) plus 3 failures that the runner never reached.
- Every regression test fails on the pre-fix tree and passes after: the segment test reproduces `DTypeMismatch { expected: C64, actual: F64 }` with the guards disabled, the eigh test reports `expected 0.4, got 0.88` with #1835's form, and the placement test reports `left: Placement { UnpinnedHost, cpu_affinity: None }` before the handover fix.
- `cargo test -p tenferro-tensor --lib` (283), `-p tenferro-cpu --lib` (544), `-p tenferro-runtime --lib` (416), `-p tenferro-ad` (95 + 354/1), `-p tenferro-linalg --features autodiff` (185 + 278 + 2 + 199) pass. The two failures are trybuild UI contracts (`tenferro-tensor storage_ui_compile_contracts`, `tenferro-ad eager_backend_capability_boundary`) that fail identically on the unmodified base commit: this environment routes rustc through kache, which rewrites the diagnostic path to `/kache/home/projects/...`, so the checked-in `.stderr` expectations cannot match. Recorded as pre-existing, not batch-caused.
- The gated pre-existing failures are the only residual risk in this batch: no verification of the region path on CUDA/WebGPU was possible here (hosted CI owns the backend matrix), and `plan_matches_input_dtypes` makes every backend decline the same mixed-dtype regions instead of only the CPU kernel erroring on them.
